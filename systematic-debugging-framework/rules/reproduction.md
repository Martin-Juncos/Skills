# Reproduction

Buscar "repro mínima":
- entorno exacto (local/CI/prod)
- datos mínimos (usuario, payload, endpoint)
- pasos enumerados
- expected vs actual

Si no hay repro:
- crear prueba aislada (endpoint mock / componente mínimo).
